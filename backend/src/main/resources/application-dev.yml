# ============================================================
# 「懒得想」LazyThink — 开发环境配置
# ============================================================
# 本文件提交到Git，不含生产密钥
# ============================================================

spring:
  datasource:
    # MySQL连接地址（数据库名：lazythink）
    url: jdbc:mysql://192.168.81.100:3306/lazythink?useUnicode=true&characterEncoding=utf8mb4&serverTimezone=Asia/Shanghai
    # 数据库用户名
    username: root
    # 数据库密码（也可通过环境变量 MYSQL_PASSWORD 覆盖）
    password: ${MYSQL_PASSWORD:root123}
    driver-class-name: com.mysql.cj.jdbc.Driver
